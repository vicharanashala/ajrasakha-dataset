# Backend API URL (for production or remote backend)
# VITE_API_URL=http://localhost:3000/api

# Set to "false" to hide/disable the Google Sign-In buttons when the backend
# also has GOOGLE_AUTH_ENABLED=false (see .env.development.example). Defaults
# to enabled when unset.
# VITE_GOOGLE_AUTH_ENABLED=true
